{
  "name": "teste-devops-Act",
  "version": "1.0.0",
  "description": "Aplicação web simples para CI/CD",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",  // Executa o servidor
    "test": "jest",
    "build": "echo 'Nenhuma construção necessária'"  // Apenas uma mensagem simples para o build
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "jest": "^29.6.1",
    "supertest": "^6.3.3"
  }
}
