{
  "name": "check-linter-action",
  "version": "1.0.0",
  "description": "GitHub Action para validar arquivos e executar linter",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "prepare": "npx @vercel/ncc build index.ts -o dist"
  },
  "dependencies": {
    "@actions/core": "^1.10.0"
  },
  "devDependencies": {
    "typescript": "^4.9.5",
    "@vercel/ncc": "^0.34.0",
    "eslint": "^8.40.0"
  }
}